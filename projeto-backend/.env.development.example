# ============================================
# ENVIRONMENT: DEVELOPMENT
# ============================================
# Este arquivo contém as configurações para desenvolvimento local

# Identificador do ambiente
ENVIRONMENT=development

# MongoDB Configuration
MONGO_URL=mongodb://localhost:27017
DB_NAME=gestao_organizacional

# JWT Configuration
JWT_SECRET=dev-secret-key-not-for-production

# CORS Configuration
# URLs permitidas para fazer requisições à API (localhost)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Frontend URL (para links de recuperação de senha)
FRONTEND_URL=http://localhost:3000

# Email Configuration (opcional para dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@localhost

# Superadmin Configuration
SUPERADMIN_EMAIL=superadmin@sistema.com
SUPERADMIN_PASSWORD=superadmin123
