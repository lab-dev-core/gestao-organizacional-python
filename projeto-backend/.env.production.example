# ============================================
# ENVIRONMENT: PRODUCTION
# ============================================
# Este arquivo contém as configurações para o ambiente de produção
# Frontend URL: https://app.dev-core.online
# Backend URL: https://back.dev-core.online

# Identificador do ambiente
ENVIRONMENT=production

# MongoDB Configuration
# IMPORTANTE: Altere a senha e o host para seus valores reais
MONGO_URL=mongodb://admin:SUA_SENHA_AQUI@localhost:27017/
DB_NAME=gestao_organizacional

# JWT Configuration
# IMPORTANTE: Use uma chave secreta forte e única em produção!
# Gere com: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=your-super-secret-key-change-in-production

# CORS Configuration
# URLs permitidas para fazer requisições à API (separadas por vírgula)
CORS_ORIGINS=https://app.dev-core.online

# Frontend URL (para links de recuperação de senha)
FRONTEND_URL=https://app.dev-core.online

# Email Configuration (para recuperação de senha)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@dev-core.online

# Superadmin Configuration
# IMPORTANTE: Altere essas credenciais em produção!
SUPERADMIN_EMAIL=superadmin@sistema.com
SUPERADMIN_PASSWORD=superadmin123
